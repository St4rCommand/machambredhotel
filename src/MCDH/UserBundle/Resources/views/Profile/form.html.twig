<div class="well">
	{{ form_start(form,{}) }}
	{{ form_errors(form) }}
	
	<fieldset>
		<legend>Coordonnées</legend>
		
		<div class="form-group row">
			{{ form_label(form.address, "Adresse", {'label_attr': {'class': 'col-lg-offset-1 col-sm-2 control-label'}}) }}
			
			<div class="col-sm-4">
				{{ form_widget(form.address, {'attr': {'class': 'form-control'}}) }}
			</div>
			
			{{ form_errors(form.address, {'attr': {'class': 'col-sm-3'}}) }}
		</div>
	
	<fieldset>
		<legend>Validation</legend>
		<div class="form-group row">
			{{ form_widget(form.save, {'attr': {'class': 'col-lg-offset-6 btn btn-primary'}}) }}
		</div>
	</fieldset>
			
	{{ form_rest(form) }}
	{{ form_end(form) }}
</div>